## Feature

<!-- Quelle feature du workshop implémentes-tu ? Lien vers l'issue -->

Closes #

## Checklist

- [ ] Testé en local (`npm run dev`)
- [ ] Le board existant fonctionne toujours (les autres colonnes/cartes ne sont pas cassées)
- [ ] `npm run build` et `npm run lint` passent en local
- [ ] Branche à jour avec `main` (rebase fait si besoin), pas de conflit non résolu
